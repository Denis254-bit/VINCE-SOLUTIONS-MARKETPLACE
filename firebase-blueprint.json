{
  "entities": {
    "consultation": {
      "title": "Corporate Consultation",
      "description": "Enterprise consultation scheduled by corporate users.",
      "type": "object",
      "properties": {
        "userUid": {
          "type": "string",
          "description": "Firebase Auth UID of the consultant"
        },
        "userEmail": {
          "type": "string",
          "format": "email",
          "description": "Email of the consultant"
        },
        "title": {
          "type": "string",
          "description": "Title of the consultation session"
        },
        "description": {
          "type": "string",
          "description": "Brief description of the consultation scope"
        },
        "clientEmail": {
          "type": "string",
          "format": "email",
          "description": "Target client email address of the consultation recipient"
        },
        "scheduledAt": {
          "type": "string",
          "format": "date-time",
          "description": "ISO date-time string of when the consultation is scheduled to occur"
        },
        "createdTimestamp": {
          "type": "string",
          "format": "date-time",
          "description": "ISO date-time when this consultation record was logged"
        }
      },
      "required": ["userUid", "userEmail", "title", "scheduledAt", "createdTimestamp"]
    }
  },
  "firestore": {
    "/consultations/{consultationId}": {
      "schema": "consultation",
      "description": "Collection of enterprise consultations scheduled by corporate users."
    }
  }
}
